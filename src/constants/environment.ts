// import { config as envConfig } from "dotenv";
// envConfig();

// const env = process.env;

// const envVars: { [key: string]: any } = {};

// export const getVariable = (name: string) => {
// 	return envVars[name];
// };

export const FULLSLATE_URL = "https://gcmtaxpros.fullslate.com/employees/4351";
